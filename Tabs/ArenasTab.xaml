<UserControl x:Class="ArenaFights.Tabs.ArenasTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1200" d:DesignHeight="700"
             Background="Transparent">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Pasek akcji -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Nowa arena" Width="120" Margin="0,0,8,0" Click="ArenaNew_Click"/>
            <Button Content="Edytuj" Width="120" Margin="0,0,8,0" Click="ArenaEdit_Click"/>
            <Button Content="Usuń" Width="120" Click="ArenaDelete_Click"/>
        </StackPanel>

        <!-- Lista aren -->
        <DataGrid x:Name="ArenyGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="#F2F2F2"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  MinRowHeight="100"
                  SelectionMode="Single">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Setter Property="MaxHeight" Value="150"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <!-- Nazwa -->
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}" Width="200"/>

                <!-- Cechy -->
                <DataGridTemplateColumn Header="Cechy" Width="150" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding Cechy}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="2" LastChildFill="False" Background="Transparent">
                                            <Border Width="40" Height="40"
                                                    Background="Transparent"
                                                    Margin="0,0,4,0"
                                                    VerticalAlignment="Center">
                                                <Image Source="{Binding IkonaPathAbsolute}"
                                                       Stretch="Uniform"
                                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                            </Border>
                                            <TextBlock Text="{Binding Nazwa}" VerticalAlignment="Center"/>
                                        </DockPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Podgląd -->
                <DataGridTemplateColumn Header="Podgląd" Width="650" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Height="200"
                                    Background="Transparent"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center"
                                    SnapsToDevicePixels="True"
                                    ClipToBounds="True">
                                <Image Source="{Binding MiniaturaPathAbsolute}"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
