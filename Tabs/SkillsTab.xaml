<UserControl x:Class="ArenaFights.Tabs.SkillsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Pasek akcji -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Nowa umiejętność" Width="160" Margin="0,0,8,0" Click="BtnSkillNew_Click"/>
            <Button Content="Edytuj umiejętność" Width="160" Margin="0,0,8,0" Click="BtnSkillEdit_Click"/>
            <Button Content="Usuń umiejętność" Width="140" Click="BtnSkillDelete_Click"/>
        </StackPanel>

        <!-- Siatka umiejętności -->
        <ListBox x:Name="SkillsList"
                 Grid.Row="1"
                 Background="#F2F2F2"
                 BorderBrush="#DDD"
                 Padding="4"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="False">

            <!-- Stała wysokość kafelka -->
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Height" Value="100"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="2"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <!-- Panel układu: wypełnianie od góry, bez rozciągania w pionie -->
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="6" VerticalAlignment="Top"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <!-- Pojedynczy kafelek -->
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="2"
                            BorderBrush="#DDD" BorderThickness="1"
                            Background="White" Padding="4">
                        <StackPanel Orientation="Vertical"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                            <Image Source="{Binding IkonaPathAbsolute}"
                                   Width="48" Height="48"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="{Binding Nazwa}"
                                       Margin="0,6,0,0"
                                       HorizontalAlignment="Center"
                                       TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
